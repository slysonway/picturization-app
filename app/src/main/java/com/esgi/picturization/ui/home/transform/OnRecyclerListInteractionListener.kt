package com.esgi.picturization.ui.home.transform

import com.esgi.picturization.data.models.FilterEnum

interface OnRecyclerListInteractionListener {
    fun onListFragmentInteraction(position: Int)
}